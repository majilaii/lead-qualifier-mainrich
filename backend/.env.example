# ╔═══════════════════════════════════════════════════════════════╗
# ║  Lead Qualifier — Environment Configuration                  ║
# ║  Copy this file to .env and fill in your API keys            ║
# ║  At minimum, you need ONE of: KIMI_API_KEY or OPENAI_API_KEY ║
# ╚═══════════════════════════════════════════════════════════════╝

# -----------------------------------------------------------
# REQUIRED (pick at least one)
# -----------------------------------------------------------

# Moonshot AI / Kimi — RECOMMENDED (cheapest, supports vision)
# Sign up: https://platform.moonshot.cn/
KIMI_API_KEY=

# OpenAI — Fallback option
# Sign up: https://platform.openai.com/
OPENAI_API_KEY=

# -----------------------------------------------------------
# LEAD DISCOVERY (optional — only needed for test_exa.py)
# -----------------------------------------------------------

# Exa AI — Neural search to discover companies matching your ICP
# Sign up: https://dashboard.exa.ai/ ($10 free credit)
EXA_API_KEY=

# -----------------------------------------------------------
# CONTACT ENRICHMENT (optional — skip initially)
# -----------------------------------------------------------

# Waterfull.ai — Waterfall enrichment across 25+ data sources (RECOMMENDED)
# Pay-as-you-go: $0.03/credit, credits never expire, no subscription
# 1 credit = 1 email, 10 credits = 1 phone number
# Sign up: https://app.waterfull.ai/auth/signup ($10 for 100 credits)
WATERFULL_API_KEY=

# Apollo.io — Email + phone lookup (API requires PAID plan, $49/mo+)
# Free plan credits are web-UI only, NOT usable via API
# APOLLO_API_KEY=

# Hunter.io — Email finder (25 free searches/month, API INCLUDED on free plan)
# Sign up: https://hunter.io/users/sign_up → Dashboard → API tab
HUNTER_API_KEY=

# -----------------------------------------------------------
# OPTIONAL: Alternative LLM
# -----------------------------------------------------------

# Anthropic Claude (alternative vision model)
# ANTHROPIC_API_KEY=

# -----------------------------------------------------------
# PIPELINE SETTINGS (defaults are fine for most cases)
# -----------------------------------------------------------

# Max parallel leads processed at once (default 10, lower if you get rate-limited)
CONCURRENCY_LIMIT=10

# LLM model names
TEXT_MODEL=gpt-4o-mini
VISION_MODEL=kimi-k2.5-thinking

# Cost tracking
TRACK_COSTS=true

# -----------------------------------------------------------
# CHAT API SERVER (for the web-based discovery interface)
# -----------------------------------------------------------

# Port for the FastAPI chat server (default 8000)
# CHAT_SERVER_PORT=8000

# Rate limiting: max requests per minute per IP (default 30)
# CHAT_RATE_LIMIT=30
